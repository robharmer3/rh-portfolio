function SkillCard({ skill }) {
  if (skill.category === 1) {
    return (
      <div className="skills">
        <h3>{skill.category}</h3>
        <p>{skill.title}</p>
      </div>
    );
  } else if (skill.category === 2) {
    return (
      <div className="skills">
        <h3>{skill.category}</h3>
        <p>{skill.title}</p>
      </div>
    );
  } else if (skill.category === 3) {
    return (
      <div className="skills">
        <h3>{skill.category}</h3>
        <p>{skill.title}</p>
      </div>
    );
  } else if (skill.category === 4) {
    return (
      <div className="skills">
        <h3>{skill.category}</h3>
        <p>{skill.title}</p>
      </div>
    );
  } else if (skill.category === 5) {
    return (
      <div className="skills">
        <h3>{skill.category}</h3>
        <p>{skill.title}</p>
      </div>
    );
  } else if (skill.category === 6) {
    return (
      <div className="skills">
        <h3>{skill.category}</h3>
        <p>{skill.title}</p>
      </div>
    );
  } else if (skill.category === 7) {
    return (
      <div className="skills">
        <h3>{skill.category}</h3>
        <p>{skill.title}</p>
      </div>
    );
  } else if (skill.category === 8) {
    return (
      <div className="skills">
        <h3>{skill.category}</h3>
        <p>{skill.title}</p>
      </div>
    );
  }
}

export default SkillCard;
